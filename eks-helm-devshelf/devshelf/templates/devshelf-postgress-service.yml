apiVersion: v1
kind: Service
metadata:
  name: devshelf-postgres
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: devshelf
    tier: postgres
  ports:
    - port: 5432