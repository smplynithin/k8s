apiVersion: v1
kind: Service
metadata:
  name: devshelf-postgres
  namespace: devshelf-prod
spec:
  clusterIP: None
  selector:
    app: devshelf
    tier: postgres
  ports:
    - port: 5432