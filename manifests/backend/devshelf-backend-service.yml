apiVersion: v1
kind: Service
metadata:
  name: devshelf-backend
  namespace: devshelf-prod
spec:
  selector:
    app: devshelf
    tier: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP