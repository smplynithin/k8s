apiVersion: apps/v1
kind: Deployment
metadata:
  name: devshelf-backend
  labels:
    app: devshelf
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devshelf
      tier: backend
  template:
    metadata:
      labels:
        app: devshelf
        tier: backend
    spec:
      containers:
        - name: backend
          image: smplynithin/devshelf-backend:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: devshelf-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: devshelf-secrets
                  key: redis-url