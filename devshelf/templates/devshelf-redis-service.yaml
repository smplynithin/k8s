apiVersion: v1
kind: Service
metadata:
  name: devshelf-redis
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: devshelf
    tier: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP