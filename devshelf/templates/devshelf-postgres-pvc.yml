apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: devshelf-postgres-pvc
  namespace: {{ .Values.namespace }}

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: standard